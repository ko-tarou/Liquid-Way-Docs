# LiqMesh プロジェクト ルール

このリポジトリ（Hack-the-Liquid-WAY）は LiqMesh ハッカソンのオーケストレーション拠点（仕様・スライド・調整）。実コードは `../liqmesh-all/` 配下の3リポジトリにある:

- **iOS**: `../liqmesh-all/liqmesh-ios`
- **Android**: `../liqmesh-all/liqmesh`
- **Desktop**: `../liqmesh-all/liqmesh-desktop`（Tauri + React + Rust、BLE は Windows のみ）

## マルチプラットフォーム作業のルール（必須）

- LiqMesh は **iOS / Android / Desktop の3プラットフォーム**で構成される。
- 各プラットフォームの実装タスクは、**そのプラットフォーム専任のエージェント**に割り当てる。**1プラットフォーム = 1担当エージェント**。複数プラットフォームを1エージェントに混ぜない。
- **メインエージェントは横断調整・仕様確定・ゲート・報告に専念**し、実装は各専任エージェントに委譲する（メインは直接コードを書かない）。
- 運用形態:
  - **第一選択は Agent Teams**（永続的な名前付き iOS/Android/Desktop エージェント ＋ タスクボード ＋ SendMessage 連携）。
  - Agent Teams が使えないセッション（再開後に runtime が落ちて SendMessage が無い等）は、**プラットフォーム別の単発サブエージェント**で同じ「1プラットフォーム=1担当」の分担を維持する。
- **共通仕様**（BLE ワイヤ契約・UI統一など）は全プラットフォームに**同一仕様**で配り、各担当が自リポで実装する。差分が出たら必ず突き合わせて一致させる。
- 並行エージェントは worktree もしくは別リポジトリで物理分離し、同一作業ディレクトリでの同時 git 操作を避ける。
