# Experiment 001 — plain transformers, no quantization.
# LFM2/LFM2.5 architecture support requires a recent transformers (>=4.57).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
torch>=2.4
transformers>=4.57
accelerate>=1.0
